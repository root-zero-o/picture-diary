const Loading = () => {
  return (
    <div className="w-full h-full fixed top-0 left-0 bg-gray-500/20 text-whit flex items-center justify-center">
      <span className="text-white text-xl">불러오는 중 ...</span>
    </div>
  );
};

export default Loading;
